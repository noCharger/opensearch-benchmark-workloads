{
  "name": "set-calcite-enabled",
  "operation-type": "raw-request",
  "path": "/_cluster/settings",
  "method": "PUT",
  "body": {
    "persistent": {
      "plugins.calcite.enabled": "{{calcite_enabled | default('false')}}"
    }
  }
},
{
  "name": "ppl-term",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | where `log.file.path` = '/var/log/messages/birdknight'"
  }
},
{
  "name": "ppl-query-string-on-message",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | where query_string(['message'], 'shield AND carp AND shark')"
  }
},
{
  "name": "ppl-query-string-on-message-filtered",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | where `@timestamp` >= '2023-01-01 00:00:00.000000000' and `@timestamp` < '2023-01-03 00:00:00.000000000' | where query_string(['message'], 'shield AND carp AND shark')"
  }
},
{
  "name": "ppl-query-string-on-message-filtered-sorted-num",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | where `@timestamp` >= '2023-01-01 00:00:00.000000000' and `@timestamp` < '2023-01-03 00:00:00.000000000' | where query_string(['message'], 'shield AND carp AND shark') | sort `metrics.size`"
  }
},
{
  "name": "ppl-desc_sort_timestamp",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "request-timeout": 30,
  "body": {
    "query": "source = {{index_name | default('big5')}} | sort - @timestamp"
  }
},
{
  "name": "ppl-asc_sort_timestamp",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "request-timeout": 30,
  "body": {
    "query": "source = {{index_name | default('big5')}} | sort + @timestamp"
  }
},
{
  "name": "ppl-desc_sort_with_after_timestamp",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "request-timeout": 30,
  "body": {
    "query": "source = {{index_name | default('big5')}} | where @timestamp < '2023-01-01 23:59:58.000000000' | sort - @timestamp"
  }
},
{
  "name": "ppl-sort_numeric_desc",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "request-timeout": 30,
  "body": {
    "query": "source = {{index_name | default('big5')}} | sort - metrics.size"
  }
},
{
  "name": "ppl-sort_numeric_asc",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "request-timeout": 30,
  "body": {
    "query": "source = {{index_name | default('big5')}} | sort + metrics.size"
  }
},
{
  "name": "ppl-date_histogram_hourly_agg",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | stats count() by span(`@timestamp`, 1h)"
  }
},
{
  "name": "ppl-date_histogram_minute_agg",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | where `@timestamp` >= '2023-01-01 00:00:00.000000000' and `@timestamp` < '2023-01-03 00:00:00.000000000' | stats count() by span(`@timestamp`, 1m)"
  }
},
{
  "name": "ppl-composite-date_histogram-daily",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | where `@timestamp` >= '2022-12-30 00:00:00.000000000' and `@timestamp` < '2023-01-07 12:00:00.000000000' | stats count() by span(`@timestamp`, 1d)"
  }
},
{
  "name": "ppl-range",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | where `@timestamp` >= '2023-01-01 00:00:00.000000000' and `@timestamp` < '2023-01-03 00:00:00.000000000'"
  }
},
{
  "name": "ppl-range-numeric",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | where `metrics.size` >= 1 and `metrics.size` <= 1000"
  }
},
{
  "name": "ppl-keyword-in-range",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | where `@timestamp` >= '2023-01-01 00:00:00.000000000' and `@timestamp` < '2023-01-03 00:00:00.000000000' | where `process.name` = 'kernel'"
  }
},
{
  "name": "ppl-range_field_conjunction_big_range_big_term_query",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | where `process.name` = 'systemd' | where `metrics.size` >= 1 and `metrics.size` <= 1000"
  }
},
{
  "name": "ppl-keyword-terms",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | stats count() by `aws.cloudwatch.log_stream` | sort -`count()` | head 500"
  }
},
{
  "name": "ppl-keyword-terms-low-cardinality",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | stats count() by `aws.cloudwatch.log_stream` | sort - `count()` | head 100"
  }
},
{
  "name": "ppl-composite-terms",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | where `@timestamp` >= '2023-01-02 00:00:00.000000000' and `@timestamp` < '2023-01-03 00:00:00.000000000' | stats count() by `process.name`, `cloud.region` | sort - `process.name`, + `cloud.region`"
  }
},
{
  "name": "ppl-composite_terms-keyword",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | where `@timestamp` >= '2023-01-02 00:00:00.000000000' and `@timestamp` < '2023-01-03 00:00:00.000000000' | stats count() by `process.name`, `cloud.region`, `aws.cloudwatch.log_stream` | sort - `process.name`, + `cloud.region`, + `aws.cloudwatch.log_stream`"
  }
},
{
  "name": "ppl-subquery_where",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | where `process.name` in [ source = {{index_name | default('big5')}} | stats count() by `process.name` | fields `process.name` ]"
  }
},
{
  "name": "ppl-left_join_timestamp",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | head 5000000 | left join on @timestamp = @timestamp [source = {{index_name | default('big5')}} | where `aws.cloudwatch.log_stream` = 'indigodagger' | stats count() by span(@timestamp, 1h)]"
  }
},
{
  "name": "ppl-semi_join_process",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | semi join on `process.name` = `process.name` [source = {{index_name | default('big5')}} | where `metrics.size` > 1000 | stats count() by `process.name`]"
  }
}