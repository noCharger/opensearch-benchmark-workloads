{
  "name": "ppl-left_join_timestamp",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | head 5000000 | left join on @timestamp = @timestamp [source = {{index_name | default('big5')}} | where `aws.cloudwatch.log_stream` = 'indigodagger' | stats count() by span(@timestamp, 1h)]"
  }
},
{
  "name": "ppl-semi_join_process",
  "operation-type": "raw-request",
  "path": "/_plugins/_ppl",
  "method": "POST",
  "body": {
    "query": "source = {{index_name | default('big5')}} | semi join on `process.name` = `process.name` [source = {{index_name | default('big5')}} | where `metrics.size` > 1000 | stats count() by `process.name`]"
  }
}